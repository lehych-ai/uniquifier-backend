# Minimal backend deps. The heavy lifting (Flux.2 + Wan2.2) runs inside ComfyUI,
# which brings its own torch/diffusers/etc. via comfy_setup.sh. The FastAPI
# backend only orchestrates: extract a frame, drive ComfyUI over HTTP, serve files.
fastapi
uvicorn[standard]
python-multipart
httpx
requests
Pillow
sse-starlette
